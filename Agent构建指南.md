# 乡村文旅多Agent协同系统构建指南

## 目录

1. [任务与问题分析](#一任务与问题分析)
2. [多Agent协同架构设计](#二多agent协同架构设计)
3. [六大问题解决方案架构](#三六大问题解决方案架构)
4. [训练流程详解](#四训练流程详解)
5. [评估指标体系与使用方法](#五评估指标体系与使用方法)
6. [测试与运行指南](#六测试与运行指南)
7. [实施路线图](#七实施路线图)

---

## 一、任务与问题分析

### 1.1 核心任务

| 任务 | 目标 | 关键挑战 |
|------|------|---------|
| ① 构建旅游垂直领域大模型 | 专注旅行规划，性能超越通用模型 | 领域知识注入、任务适配 |
| ② 针对乡村场景 | 文化+旅游融合，乡村特色落地 | 文化元素体系化、场景差异化 |
| ③ 性能优于现有模型 | 评估指标全面超越基线 | 评估体系设计、持续优化 |

### 1.2 六大问题梳理

| 问题 | 核心挑战 | 解决思路 |
|------|---------|---------|
| ① 数据格式不统一 | 新数据与训练集格式不一致，统一格式不现实 | 数据适配层 + 动态Schema映射 |
| ② 性能指标提升 | 需要超越现有模型 | 多阶段训练 + 专项优化 |
| ③ 乡村文化落地 | 文化元素如何融入旅游规划 | 文化知识图谱 + 文化Agent |
| ④ 外部API集成 | 携程、高德等实时数据接入 | MCP服务架构 + 工具Agent |
| ⑤ 知识库构建 | RAG系统提升准确性和丰富度 | 多知识库 + 检索Agent |
| ⑥ 多模态处理 | 图片、视频等格式未知 | 模块化处理 + 多模态Agent |

---

## 二、多Agent协同架构设计

### 2.1 整体架构概览

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              用户交互层                                      │
│                    (文本 / 语音 / 图片 / 视频输入)                           │
└─────────────────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         🎯 主控Agent (Orchestrator)                          │
│                                                                             │
│   职责：任务理解 → 任务分解 → Agent调度 → 结果整合 → 输出生成               │
│                                                                             │
│   核心能力：                                                                │
│   • 用户意图识别        • 任务规划与分解                                    │
│   • 多Agent协调调度     • 冲突检测与解决                                    │
│   • 结果融合与优化      • 质量把控与兜底                                    │
└─────────────────────────────────────────────────────────────────────────────┘
                                     │
            ┌────────────┬───────────┼───────────┬────────────┐
            ▼            ▼           ▼           ▼            ▼
┌───────────────┐ ┌───────────────┐ ┌───────────────┐ ┌───────────────┐ ┌───────────────┐
│ 🧭 规划Agent  │ │ 🏛️ 文化Agent  │ │ 🔧 工具Agent  │ │ 📚 检索Agent  │ │ 🖼️ 多模态Agent │
│               │ │               │ │               │ │               │ │               │
│ • 行程规划    │ │ • 文化推荐    │ │ • API调用     │ │ • 知识检索    │ │ • 图像理解    │
│ • 时间安排    │ │ • 故事生成    │ │ • 数据获取    │ │ • RAG增强     │ │ • 视频分析    │
│ • 路线优化    │ │ • 体验设计    │ │ • 实时信息    │ │ • 上下文构建  │ │ • 语音转换    │
│ • 预算分配    │ │ • 文化融入    │ │ • 格式转换    │ │ • 相关性排序  │ │ • 内容描述    │
└───────────────┘ └───────────────┘ └───────────────┘ └───────────────┘ └───────────────┘
            │            │           │           │            │
            └────────────┴───────────┼───────────┴────────────┘
                                     ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            基础设施层                                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐ │
│  │  大语言模型  │  │  向量数据库  │  │  知识图谱   │  │     MCP服务        │ │
│  │ (DeepSeek)  │  │  (Milvus)   │  │  (文化库)   │  │  (携程/高德/本地)  │ │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 各Agent职责详解

#### 🎯 主控Agent (Orchestrator Agent)

**定位**：系统大脑，负责全局协调

**核心职责**：

| 职责 | 说明 |
|------|------|
| 意图识别 | 理解用户查询，识别旅行需求（目的地、时间、预算、偏好等） |
| 任务分解 | 将复杂需求拆解为子任务，分配给专业Agent |
| 调度协调 | 决定Agent执行顺序，管理并行/串行执行 |
| 结果整合 | 汇总各Agent输出，消除冲突，生成最终计划 |
| 质量把控 | 检查输出质量，必要时触发重试或降级 |

**工作流程**：
```
用户输入 → 意图解析 → 任务分解 → Agent调度 → 结果收集 → 冲突检测 → 结果融合 → 输出生成
```

---

#### 🧭 规划Agent (Planning Agent)

**定位**：行程规划专家

**核心职责**：

| 职责 | 输入 | 输出 |
|------|------|------|
| 行程规划 | 目的地、天数、偏好 | 每日行程框架 |
| 时间安排 | 景点列表、开放时间 | 具体时间表 |
| 路线优化 | 景点坐标、交通方式 | 最优游览路线 |
| 预算分配 | 总预算、各项成本 | 分项预算方案 |

**决策逻辑**：
- 根据用户偏好（轻松/适中/紧凑）调整每日活动密度
- 考虑景点开放时间、地理位置优化路线
- 平衡景点类型（自然/人文/休闲）
- 确保交通衔接合理

---

#### 🏛️ 文化Agent (Culture Agent)

**定位**：乡村文化专家，**解决问题③**

**核心职责**：

| 职责 | 说明 |
|------|------|
| 文化元素匹配 | 根据目的地匹配当地文化特色（民俗、非遗、红色文化等） |
| 文化故事生成 | 为景点/活动生成文化背景故事，提升体验感 |
| 文化体验设计 | 推荐文化体验活动（手工艺、民俗节庆、农事体验等） |
| 文化融入优化 | 在行程中合理融入文化元素，确保自然不突兀 |

**文化元素体系**：
```
                        乡村文化元素
                             │
    ┌────────┬────────┬──────┴──────┬────────┬────────┐
    ▼        ▼        ▼             ▼        ▼        ▼
 物质文化  非遗文化   生态文化    红色文化  民族文化  产业文化
    │        │        │             │        │        │
 古村落    传统技艺   田园风光    革命遗址  民族村寨  特色农产
 传统民居  民间艺术   农耕体验    红色故事  民族服饰  传统手工
 文物古迹  地方戏曲   采摘活动    纪念场馆  民族歌舞  乡村电商
```

---

#### 🔧 工具Agent (Tool Agent)

**定位**：外部数据接口，**解决问题④**

**核心职责**：

| 职责 | 数据源 | 用途 |
|------|--------|------|
| 交通查询 | 携程API、本地数据 | 航班、火车时刻与价格 |
| 住宿查询 | 携程API、本地数据 | 酒店、民宿信息 |
| 景点查询 | 高德API、本地数据 | POI信息、评分评价 |
| 天气查询 | 高德API | 目的地天气预报 |
| 路线规划 | 高德API | 导航路线、距离时间 |
| 实时信息 | 各类API | 门票价格、开放状态 |

**MCP服务架构**：
```
                    ┌─────────────────┐
                    │   工具Agent     │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │   MCP网关       │
                    │  (统一接口)      │
                    └────────┬────────┘
                             │
        ┌──────────┬─────────┼─────────┬──────────┐
        ▼          ▼         ▼         ▼          ▼
   ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
   │携程服务 │ │高德服务 │ │天气服务 │ │本地数据 │ │ 缓存层  │
   └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘
```

---

#### 📚 检索Agent (Retrieval Agent)

**定位**：知识检索专家，**解决问题⑤**

**核心职责**：

| 职责 | 说明 |
|------|------|
| 知识检索 | 从向量数据库检索相关信息 |
| 上下文构建 | 为LLM构建富含信息的上下文 |
| 相关性排序 | 对检索结果按相关性排序 |
| 多库联合检索 | 同时检索景点库、文化库、攻略库等 |

**RAG系统架构**：
```
用户查询
    │
    ▼
┌─────────────────────────────────────────┐
│              检索Agent                   │
│  ┌───────────┐  ┌───────────────────┐   │
│  │ 查询理解   │──▶│   查询改写/扩展   │   │
│  └───────────┘  └─────────┬─────────┘   │
│                           │             │
│                           ▼             │
│  ┌─────────────────────────────────┐    │
│  │         多路召回                 │    │
│  │  ┌─────┐ ┌─────┐ ┌─────┐       │    │
│  │  │向量 │ │关键词│ │知识 │       │    │
│  │  │检索 │ │检索 │ │图谱 │       │    │
│  │  └──┬──┘ └──┬──┘ └──┬──┘       │    │
│  │     └───────┼───────┘          │    │
│  └─────────────┼──────────────────┘    │
│                ▼                        │
│  ┌─────────────────────────────────┐    │
│  │   重排序 + 去重 + Top-K选择     │    │
│  └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
    │
    ▼
检索结果 → 构建上下文 → 输入LLM
```

**知识库分类**：

| 知识库 | 内容 | 用途 |
|--------|------|------|
| 景点库 | 景点介绍、评价、攻略 | 景点推荐与介绍 |
| 文化库 | 文化元素、民俗故事、非遗介绍 | 文化内容生成 |
| 乡村库 | 乡村介绍、特色活动、美食 | 乡村场景支持 |
| 攻略库 | 游记、经验分享、注意事项 | 实用建议生成 |

---

#### 🖼️ 多模态Agent (Multimodal Agent)

**定位**：多模态数据处理，**解决问题⑥**

**核心职责**：

| 输入类型 | 处理方式 | 输出 |
|----------|----------|------|
| 图片 | 视觉模型理解 | 场景描述、景点识别 |
| 视频 | 关键帧提取+理解 | 内容摘要、亮点提取 |
| 语音 | ASR转写 | 文本查询 |
| 混合 | 多模态融合 | 统一文本表示 |

**处理流程**：
```
多模态输入
    │
    ├── 图片 ──▶ 视觉编码器 ──▶ 图像描述
    │
    ├── 音频 ──▶ ASR模型 ────▶ 文本转写
    │
    ├── 视频 ──▶ 视频理解 ───▶ 内容摘要
    │
    └── 文本 ──▶ 直接传递 ───▶ 原始文本
                     │
                     ▼
              统一文本表示 → 主控Agent
```

---

### 2.3 Agent协同工作流程

#### 典型查询处理流程

```
用户: "我想从上海去婺源看油菜花，3天2晚，预算3000，想体验当地文化"

Step 1: 主控Agent 接收查询，解析意图
        ├── 目的地: 婺源
        ├── 出发地: 上海  
        ├── 天数: 3天2晚
        ├── 预算: 3000元
        └── 偏好: 文化体验、油菜花

Step 2: 主控Agent 分解任务，调度Agent
        │
        ├──▶ 检索Agent: 检索婺源景点、文化信息
        │
        ├──▶ 工具Agent: 查询交通、住宿、天气
        │
        └──▶ 文化Agent: 匹配当地文化元素

Step 3: 各Agent并行执行，返回结果
        │
        ├── 检索Agent返回: 篁岭、江湾、李坑等景点信息
        │
        ├── 工具Agent返回: 高铁班次、民宿列表、天气预报
        │
        └── 文化Agent返回: 徽派建筑、油纸伞制作、徽剧等

Step 4: 主控Agent 调度规划Agent
        │
        └──▶ 规划Agent: 综合信息生成行程方案

Step 5: 主控Agent 结果整合
        │
        ├── 检查完整性（交通✓ 住宿✓ 景点✓ 餐饮✓）
        ├── 检查可行性（时间合理✓ 预算符合✓）
        ├── 融入文化元素
        └── 生成最终输出

Step 6: 输出完整旅行计划
```

#### Agent调度策略

| 场景 | 调度策略 |
|------|---------|
| 简单查询（仅景点推荐） | 检索Agent → 规划Agent |
| 标准查询（完整规划） | 并行[检索+工具+文化] → 规划Agent |
| 文化专题查询 | 文化Agent → 检索Agent → 规划Agent |
| 多模态查询（带图片） | 多模态Agent → 标准流程 |
| 实时信息查询 | 工具Agent优先 |

---

## 三、六大问题解决方案架构

### 3.1 问题①：数据格式不统一

#### 解决方案：数据适配层架构

```
                        外部数据源
                            │
    ┌───────────┬───────────┼───────────┬───────────┐
    ▼           ▼           ▼           ▼           ▼
┌───────┐  ┌───────┐  ┌───────┐  ┌───────┐  ┌───────┐
│携程API│  │高德API│  │新乡村 │  │多模态 │    │训练集  │
│ JSON  │  │ JSON  │  │ 数据  │  │ 数据  │  │  CSV  │
└───┬───┘  └───┬───┘  └───┬───┘  └───┬───┘  └───┬───┘
    │          │          │          │          │
    ▼          ▼          ▼          ▼          ▼
┌─────────────────────────────────────────────────────┐
│                   数据适配层                         │
│  ┌─────────────────────────────────────────────┐   │
│  │              适配器注册表                     │   │
│  │  携程适配器 │ 高德适配器 │ 通用适配器 │ ...  │   │
│  └─────────────────────────────────────────────┘   │
│                        │                           │
│  ┌─────────────────────▼─────────────────────┐     │
│  │            Schema映射引擎                  │     │
│  │  • 字段映射 (name ↔ poiName ↔ 景点名称)   │     │
│  │  • 类型转换 (string → float)              │     │
│  │  • 默认值填充                              │     │
│  └─────────────────────┬─────────────────────┘     │
│                        │                           │
│  ┌─────────────────────▼─────────────────────┐     │
│  │              数据验证器                    │     │
│  │  • 必填字段检查                            │     │
│  │  • 数值范围校验                            │     │
│  │  • 格式一致性验证                          │     │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │  统一内部格式  │
                    │ (Unified Schema)│
                    └───────────────┘
```

#### 核心设计原则

1. **适配器模式**：每种数据源一个适配器，互不影响
2. **Schema优先**：定义统一内部格式，所有数据转换到此格式
3. **动态映射**：支持运行时配置字段映射，无需修改代码
4. **向下兼容**：新字段使用默认值，不影响现有逻辑

---

### 3.2 问题②：性能指标提升

#### 解决方案：多阶段训练架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                     多阶段训练流程                                   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  阶段1: SFT (监督微调)                                              │
│  ┌───────────────────────────────────────────────────────────┐     │
│  │  输入: train.json (查询→计划 对)                           │     │
│  │  目标: 学习基本规划能力                                     │     │
│  │  方法: LoRA微调DeepSeek                                    │     │
│  │  预期: 基础规划能力 +20-30%                                 │     │
│  └───────────────────────────────────────────────────────────┘     │
│                              │                                      │
│                              ▼                                      │
│  阶段2: RM (奖励模型训练)                                           │
│  ┌───────────────────────────────────────────────────────────┐     │
│  │  输入: 对比数据 (好计划 vs 差计划)                          │     │
│  │  目标: 学习评估计划质量                                     │     │
│  │  特殊: 加入乡村文化奖励因子                                 │     │
│  │  预期: 质量判别能力                                         │     │
│  └───────────────────────────────────────────────────────────┘     │
│                              │                                      │
│                              ▼                                      │
│  阶段3: RLHF (强化学习微调) [可选]                                  │
│  ┌───────────────────────────────────────────────────────────┐     │
│  │  输入: 用户查询                                            │     │
│  │  目标: 优化生成策略                                         │     │
│  │  方法: PPO算法 + RM打分                                    │     │
│  │  预期: 综合质量 +10-20%                                    │     │
│  └───────────────────────────────────────────────────────────┘     │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

### 3.3 问题③：乡村文化落地

#### 解决方案：文化Agent + 知识图谱

```
┌─────────────────────────────────────────────────────────────────────┐
│                      文化知识体系架构                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                     文化知识图谱                              │   │
│  │                                                               │   │
│  │    ┌─────────┐    所属    ┌─────────┐    特色    ┌─────────┐ │   │
│  │    │  乡村   │──────────▶│  文化   │──────────▶│  活动   │ │   │
│  │    └─────────┘           └─────────┘           └─────────┘ │   │
│  │         │                     │                     │       │   │
│  │         │ 拥有               │ 包含               │ 体验   │   │
│  │         ▼                     ▼                     ▼       │   │
│  │    ┌─────────┐           ┌─────────┐           ┌─────────┐ │   │
│  │    │  景点   │           │  元素   │           │  美食   │ │   │
│  │    └─────────┘           └─────────┘           └─────────┘ │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                     文化Agent工作流                          │   │
│  │                                                               │   │
│  │  用户偏好 ──▶ 文化类型匹配 ──▶ 元素推荐 ──▶ 活动设计 ──▶ 融入行程 │
│  │                                                               │   │
│  │  例: "体验传统文化"                                           │   │
│  │       → 匹配"非遗文化"                                       │   │
│  │       → 推荐"徽派建筑、油纸伞"                                │   │
│  │       → 设计"手工体验活动"                                   │   │
│  │       → 融入Day2下午行程                                     │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

---

### 3.4 问题④：MCP服务构建

#### 解决方案：统一MCP网关

```
┌─────────────────────────────────────────────────────────────────────┐
│                        MCP服务架构                                   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                      MCP统一网关                             │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────────────┐   │   │
│  │  │  认证   │ │  限流   │ │  日志   │ │     缓存        │   │   │
│  │  │  鉴权   │ │  控制   │ │  记录   │ │   (Redis)       │   │   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                      │
│         ┌────────────────────┼────────────────────┐                │
│         ▼                    ▼                    ▼                │
│  ┌─────────────┐      ┌─────────────┐      ┌─────────────┐        │
│  │ 携程MCP服务 │      │ 高德MCP服务 │      │ 本地MCP服务 │        │
│  ├─────────────┤      ├─────────────┤      ├─────────────┤        │
│  │ • 景点搜索  │      │ • 路线规划  │      │ • 景点查询  │        │
│  │ • 酒店查询  │      │ • POI搜索   │      │ • 住宿查询  │        │
│  │ • 机票查询  │      │ • 天气查询  │      │ • 餐厅查询  │        │
│  │ • 火车票    │      │ • 地理编码  │      │ • 交通查询  │        │
│  └─────────────┘      └─────────────┘      └─────────────┘        │
│                                                                     │
│  调用策略:                                                          │
│  • 优先级: 携程API > 高德API > 本地数据                            │
│  • 降级: API失败时自动降级到本地数据                               │
│  • 缓存: 热点数据缓存，减少API调用                                 │
└─────────────────────────────────────────────────────────────────────┘
```

---

### 3.5 问题⑤：RAG知识库

#### 解决方案：多知识库RAG架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                        RAG系统架构                                   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                     知识库层                                 │   │
│  │                                                               │   │
│  │   ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐│   │
│  │   │  景点库   │  │  文化库   │  │  乡村库   │  │  攻略库   ││   │
│  │   │ • 介绍    │  │ • 元素    │  │ • 介绍    │  │ • 游记    ││   │
│  │   │ • 评价    │  │ • 故事    │  │ • 活动    │  │ • 经验    ││   │
│  │   │ • 攻略    │  │ • 节庆    │  │ • 美食    │  │ • 提示    ││   │
│  │   └───────────┘  └───────────┘  └───────────┘  └───────────┘│   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                      │
│                              ▼                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                     检索层                                   │   │
│  │                                                               │   │
│  │   用户查询 ──▶ 查询改写 ──▶ 多库检索 ──▶ 重排序 ──▶ Top-K  │   │
│  │                                                               │   │
│  │   检索方式:                                                   │   │
│  │   • 向量检索 (语义相似)                                      │   │
│  │   • 关键词检索 (精确匹配)                                    │   │
│  │   • 混合检索 (加权融合)                                      │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                      │
│                              ▼                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                     生成层                                   │   │
│  │                                                               │   │
│  │   检索结果 ──▶ 上下文构建 ──▶ Prompt组装 ──▶ LLM生成      │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

---

### 3.6 问题⑥：多模态处理

#### 解决方案：模块化多模态架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                      多模态处理架构                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│                       多模态输入                                    │
│                          │                                          │
│    ┌─────────┬───────────┼───────────┬─────────┐                   │
│    ▼         ▼           ▼           ▼         ▼                   │
│ ┌──────┐ ┌──────┐   ┌──────┐   ┌──────┐   ┌──────┐                │
│ │ 文本 │ │ 图片 │   │ 音频 │   │ 视频 │   │ 混合 │                │
│ └──┬───┘ └──┬───┘   └──┬───┘   └──┬───┘   └──┬───┘                │
│    │        │          │          │          │                     │
│    │        ▼          ▼          ▼          │                     │
│    │   ┌─────────┐ ┌─────────┐ ┌─────────┐   │                     │
│    │   │视觉模型 │ │ASR模型  │ │视频理解 │   │                     │
│    │   │(BLIP等)│ │(Whisper)│ │         │   │                     │
│    │   └────┬────┘ └────┬────┘ └────┬────┘   │                     │
│    │        │          │          │          │                     │
│    │        ▼          ▼          ▼          │                     │
│    │   ┌─────────┐ ┌─────────┐ ┌─────────┐   │                     │
│    │   │图像描述 │ │文本转写 │ │内容摘要 │   │                     │
│    │   └────┬────┘ └────┬────┘ └────┬────┘   │                     │
│    │        │          │          │          │                     │
│    └────────┴──────────┴──────────┴──────────┘                     │
│                          │                                          │
│                          ▼                                          │
│                  ┌───────────────┐                                  │
│                  │ 统一文本表示  │                                  │
│                  └───────────────┘                                  │
│                          │                                          │
│                          ▼                                          │
│                  ┌───────────────┐                                  │
│                  │   主控Agent   │                                  │
│                  └───────────────┘                                  │
│                                                                     │
│  设计原则:                                                          │
│  • 模块化: 各模态处理器独立，可插拔                                │
│  • 格式适配: 自动检测输入格式，选择处理器                          │
│  • 统一输出: 所有模态转换为文本，便于LLM处理                       │
│  • 渐进支持: 先支持图片，再扩展音频、视频                          │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 四、训练流程详解

### 4.1 训练环境准备

#### 硬件要求

| 阶段 | 最低配置 | 推荐配置 |
|------|---------|---------|
| SFT训练 | 4×A100-40G | 8×A100-80G |
| RM训练 | 4×A100-40G | 8×A100-40G |
| RLHF训练 | 8×A100-40G | 16×A100-80G |

#### 软件环境

```bash
# 基础环境
Python >= 3.10
CUDA >= 11.8
PyTorch >= 2.0

# 核心依赖
transformers >= 4.36.0
deepspeed >= 0.12.0
peft >= 0.7.0  # LoRA支持
```

### 4.2 阶段一：SFT训练

#### 步骤1：数据准备

**数据来源**：`TripTailor/data/train.json`

**数据格式**：
```json
{
    "instruction": "用户查询文本",
    "input": "",
    "output": "生成的旅行计划"
}
```

**数据增强（可选）**：
- 查询改写：生成语义相同但表达不同的查询
- 乡村元素添加：在查询中加入乡村文化偏好
- 负样本生成：生成有问题的计划用于对比学习

#### 步骤2：运行训练

```bash
# 进入训练目录
cd TripTailor/TripTailor/Rm/DeepSpeed-Chat

# 修改配置（run_sft_train.sh）
# - model_name_or_path: 基础模型路径（如deepseek-ai/DeepSeek-V2）
# - data_path: 训练数据路径
# - output_dir: 输出目录

# 启动训练
bash run_sft_train.sh output/sft_model 2

# 参数说明：
# - output/sft_model: 模型输出目录
# - 2: ZeRO Stage (推荐2或3)
```

#### 关键超参数

| 参数 | 推荐值 | 说明 |
|------|-------|------|
| learning_rate | 2e-5 | 学习率 |
| num_train_epochs | 3 | 训练轮数 |
| per_device_train_batch_size | 4 | 单卡批次大小 |
| gradient_accumulation_steps | 4 | 梯度累积步数 |
| max_seq_len | 4096 | 最大序列长度 |
| lora_r | 32 | LoRA秩 |
| lora_alpha | 64 | LoRA alpha |

#### 步骤3：验证训练结果

```bash
# 检查训练日志
tail -f output/sft_model/training.log

# 关键指标：
# - loss: 应该持续下降
# - eval_loss: 验证集loss，监控过拟合
```

### 4.3 阶段二：RM训练

#### 步骤1：数据准备

**数据格式**：对比数据对（chosen vs rejected）

```json
{
    "query": "用户查询",
    "chosen": "好的旅行计划",
    "rejected": "差的旅行计划"
}
```

**数据生成方式**：
1. 使用`Rm/data/data_transform.py`转换训练数据
2. 好计划来自train.json的final_plan
3. 差计划通过打乱顺序、删除信息等方式生成

#### 步骤2：运行训练

```bash
# 进入目录
cd TripTailor/TripTailor/Rm/DeepSpeed-Chat

# 数据转换
cd ../data
python data_transform.py

# 运行RM训练
cd ../DeepSpeed-Chat
bash run_rm_train.sh output/rm_model 3

# 参数说明：
# - output/rm_model: 输出目录
# - 3: ZeRO Stage
```

#### 关键配置（run_rm_train.sh）

| 参数 | 值 | 说明 |
|------|-------|------|
| data_path | travel | 数据路径标识 |
| data_split | 0,10,0 | 训练/验证/测试比例 |
| per_device_train_batch_size | 4 | 批次大小 |
| max_seq_len | 4096 | 序列长度 |
| num_train_epochs | 2 | 训练轮数 |
| learning_rate | 1e-5 | 学习率 |

### 4.4 阶段三：RLHF训练（可选）

#### 前置条件
- 完成SFT训练，获得Actor模型
- 完成RM训练，获得Reward模型

#### 运行步骤

```bash
# 需要额外实现PPO训练脚本
# 基本流程：

# 1. 加载Actor模型（SFT后的模型）
# 2. 加载Reward模型
# 3. 生成多个候选输出
# 4. 使用RM评分
# 5. PPO更新Actor参数
```

### 4.5 训练监控

#### TensorBoard监控

```bash
# 启动TensorBoard
tensorboard --logdir=output/tensorboard --port=6006

# 浏览器访问
http://localhost:6006
```

#### 关键监控指标

| 指标 | 正常范围 | 异常处理 |
|------|---------|---------|
| train_loss | 持续下降 | 检查学习率、数据质量 |
| eval_loss | 同步下降 | 上升则可能过拟合 |
| grad_norm | < 1.0 | 过大需要梯度裁剪 |
| lr | 按schedule变化 | 检查scheduler配置 |

---

## 五、评估指标体系与使用方法

### 5.1 评估指标详解

#### 指标体系总览

```
┌─────────────────────────────────────────────────────────────────────┐
│                        评估指标体系                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  一级指标              二级指标              权重    目标值           │
│  ─────────────────────────────────────────────────────────────      │
│                                                                     │
│  功能性指标 (40%)                                                    │
│  ├── 计划完整性        交通/住宿/景点/餐饮齐全   15%    ≥95%           │
│  ├── 时间可行性        时间安排无冲突            15%    ≥90%       │
│  └── 预算符合度        总成本在预算±10%内        10%    ≥85%       │
│                                                                     │
│  质量指标 (30%)                                                     │
│  ├── 路线合理性        地理位置连贯              10%    ≥85%       │
│  ├── 信息准确性        价格/时间/位置正确        10%    ≥90%       │
│  └── 多样性            景点类型丰富              10%    ≥80%       │
│                                                                     │
│  乡村特色指标 (20%)                                                 │
│  ├── 文化融入度        包含文化元素              10%    ≥80%       │
│  └── 乡村体验          包含乡村特色活动          10%    ≥75%       │
│                                                                     │
│  用户满意度 (10%)                                                   │
│  ├── 专家评分          人工打分(1-5)             5%     ≥4.0       │
│  └── 用户评分          用户反馈(1-5)             5%     ≥4.0       │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

#### 各指标计算方法

##### 1. 计划完整性

**定义**：生成的计划是否包含必要的四大要素

**计算公式**：
```
完整性 = 包含要素数 / 4

必要要素：
- 交通安排（航班/火车）
- 住宿安排（酒店/民宿）
- 景点安排（每日景点）
- 餐饮安排（午餐/晚餐）
```

**判定逻辑**：
- 4/4 = 100%（完全完整）
- 3/4 = 75%（缺少1项）
- 2/4 = 50%（缺少2项）
- ...

##### 2. 时间可行性

**定义**：时间安排是否合理、无冲突

**检查项**：
```
- 景点开放时间：到达时间 < 关闭时间
- 游览时长合理：参考时长 ≤ 分配时长 ≤ 参考时长×2
- 交通衔接：后一活动开始时间 > 前一活动结束时间 + 交通时间
- 用餐时间：午餐11:30-14:00，晚餐17:30-20:00
```

**计算公式**：
```
时间可行性 = 无冲突时间段数 / 总时间段数
```

##### 3. 预算符合度

**定义**：实际总成本与用户预算的匹配程度

**计算公式**：
```
若 实际成本 ≤ 预算：
    预算符合度 = 1.0

若 实际成本 > 预算：
    超出比例 = (实际成本 - 预算) / 预算
    预算符合度 = max(0, 1 - 超出比例)
```

##### 4. 路线合理性

**定义**：景点安排是否符合地理位置逻辑

**检查项**：
- 同一天景点距离不超过30km
- 无明显往返折腾（A→B→A）
- 考虑交通便利性

**计算公式**：
```
路线合理性 = 合理安排天数 / 总天数
```

##### 5. 文化融入度

**定义**：计划中文化元素的体现程度

**文化关键词库**：
```
物质文化：古村、古镇、民居、宗祠、文物...
非遗文化：技艺、手工、戏曲、民间艺术...
生态文化：田园、农耕、采摘、农事...
红色文化：革命、纪念、红色、遗址...
民族文化：民族、服饰、歌舞、村寨...
```

**计算公式**：
```
文化融入度 = min(1.0, 提及文化关键词数 / 3)
```

##### 6. 乡村体验

**定义**：计划中乡村特色活动的体现程度

**乡村关键词库**：
```
住宿：农家乐、民宿、客栈...
活动：采摘、农事体验、手工制作...
美食：农家菜、土特产、地方小吃...
```

**计算公式**：
```
乡村体验 = min(1.0, 提及乡村关键词数 / 3)
```

### 5.2 评估工具使用方法

#### 步骤1：准备评估数据

```bash
# 评估所需文件
评估输入文件（模型生成结果）:
    - 路径: outputs/{model_name}_{strategy}.json
    - 格式: [{"pid": 1, "query": "...", "{model}_plan": "..."}, ...]

参考信息文件:
    - 路径: data/infomation.json
    - 格式: {"pid": {"hotel": [...], "transport": [...], ...}}

Ground Truth文件:
    - 路径: data/test.json
    - 格式: [{"pid": 1, "query": "...", "final_plan": [...], "budget": ...}, ...]
```

#### 步骤2：运行评估

```bash
# 进入评估目录
cd TripTailor/TripTailor/eval

# 修改评估配置 (eval.sh)
MODEL_NAME="deepseek_sft"        # 被评估的模型名称
STRATEGY="direct"                # 策略类型
INPUT_FILE="../outputs/deepseek_sft_direct.json"  # 输入文件
INFO_FILE="../data/infomation.json"               # 信息文件
FINAL_INFO_FILE="../data/test.json"               # 参考文件
EVAL_MODEL_NAME="gpt-4"          # 用于评估的模型（用于语义评估）

# 运行评估
bash eval.sh
```

#### 步骤3：查看评估结果

```bash
# 评估输出
输出文件: outputs/eval_{model_name}_{strategy}.json

# 结果格式
{
    "overall_scores": {
        "completeness": 0.95,
        "time_feasibility": 0.88,
        "budget_compliance": 0.82,
        "route_rationality": 0.85,
        "info_accuracy": 0.90,
        "diversity": 0.78,
        "cultural_integration": 0.72,
        "rural_experience": 0.68,
        "total_score": 0.83
    },
    "per_sample_scores": [
        {"pid": 1, "scores": {...}},
        ...
    ]
}
```

### 5.3 指标对比与分析

#### 基线对比方法

```bash
# 运行多模型评估
# 1. 对每个模型运行评估
bash eval.sh  # 模型A
# 修改MODEL_NAME后再次运行
bash eval.sh  # 模型B
bash eval.sh  # 模型C

# 2. 生成对比报告
python generate_comparison_report.py \
    --models "baseline,sft,sft_rm" \
    --output "comparison_report.md"
```

#### 对比报告示例

```
| 模型 | 完整性 | 时间可行 | 预算符合 | 文化融入 | 总分 |
|------|--------|----------|----------|----------|------|
| 基线 | 0.85   | 0.75     | 0.70     | 0.45     | 0.72 |
| SFT  | 0.95   | 0.88     | 0.82     | 0.72     | 0.84 |
| +RM  | 0.96   | 0.90     | 0.85     | 0.80     | 0.88 |

提升幅度:
- SFT vs 基线: +16.7%
- +RM vs SFT: +4.8%
- 最终 vs 基线: +22.2%
```

### 5.4 人工评估方法

#### 评估维度

| 维度 | 评分标准 | 分值范围 |
|------|---------|---------|
| 整体质量 | 计划是否完整、合理、可执行 | 1-5 |
| 信息准确性 | 信息是否准确、无明显错误 | 1-5 |
| 文化体现 | 文化元素是否自然融入 | 1-5 |
| 实用性 | 是否真的想按此计划旅行 | 1-5 |

#### 评估流程

1. **样本抽取**：从测试集随机抽取50-100个样本
2. **盲评设计**：隐藏模型名称，仅展示计划内容
3. **多人评估**：至少3名评估者，取平均分
4. **一致性检验**：计算Kappa系数，确保评估一致性

---

## 六、测试与运行指南

### 6.1 测试环境搭建

#### 依赖安装

```bash
# 进入项目目录
cd TripTailor

# 安装依赖
pip install -r requirements.txt

# 主要依赖
- pandas
- openai
- langchain
- geopy
- tqdm
```

#### 配置文件

```bash
# 设置环境变量
export OPENAI_API_KEY="your_api_key"
export OPENAI_BASE_URL="your_base_url"  # 如使用DeepSeek等

# 或在脚本中配置
API_KEY="your_api_key"
BASE_URL="https://api.deepseek.com/v1"
```

### 6.2 运行测试

#### 方式一：完整工作流测试

```bash
# 进入Agent目录
cd TripTailor/TripTailor/agents

# 运行完整工作流
bash run_TravelAgent.sh

# 配置参数 (在脚本中修改)
INPUT_FILE="../data/test.json"        # 测试数据
INPUT_INFO_FILE="../data/infomation.json"  # 信息文件
OUTPUT_PATH="./outputs/"              # 输出目录
MODEL_NAME="deepseek-chat"            # 模型名称
MODE="workflow"                       # 运行模式
```

#### 方式二：预搜索+直接规划（两阶段）

```bash
# 阶段1：预搜索
bash run_presearch.sh

# 配置
INPUT_FILE="../data/test.json"
OUTPUT_PATH="./outputs/"
OUTPUT_INFO_FILE="./outputs/presearch_info.json"
MODEL_NAME="deepseek-chat"
MODE="presearch"

# 阶段2：直接规划
bash run_TravelAgent_direct.sh

# 配置
INPUT_FILE="./outputs/direct_input.json"  # 预搜索输出
OUTPUT_PATH="./outputs/"
MODEL_NAME="deepseek-chat"
MODE="direct"
```

#### 方式三：独立Planner测试

```bash
# 进入Planner目录
cd TripTailor/TripTailor/planner

# 运行规划器
bash run_sole_planning.sh

# 配置
INPUT_FILE="../data/test.json"
SET_TYPE="test"
MODEL_NAME="deepseek-chat"
OUTPUT_DIR="./outputs/test/"
STRATEGY="direct"  # 可选: direct/cot/react/reflexion
```

### 6.3 结果整合

```bash
# 整合Agent结果
cd TripTailor/TripTailor/agents
bash combination.sh

# 配置
INPUT_FILE="../data/test.json"
OUTPUT_DIR="./outputs/"
MODEL_NAME="deepseek-chat"
SET_TYPE="test"
STRATEGY="direct"
SUBMISSION_FILE="./outputs/final_submission.json"

# 整合Planner结果
cd TripTailor/TripTailor/planner
bash combination.sh
```

### 6.4 完整测试流程

```bash
# ============ 完整测试流程 ============

# 步骤1: 环境准备
cd TripTailor
pip install -r requirements.txt
export OPENAI_API_KEY="your_key"
export OPENAI_BASE_URL="https://api.deepseek.com/v1"

# 步骤2: 运行Agent（选择一种方式）
cd TripTailor/agents

# 方式A: 完整工作流
bash run_TravelAgent.sh

# 方式B: 两阶段
bash run_presearch.sh
bash run_TravelAgent_direct.sh

# 步骤3: 整合结果
bash combination.sh

# 步骤4: 运行评估
cd ../eval
bash eval.sh

# 步骤5: 查看结果
cat outputs/eval_results.json

# =====================================
```

### 6.5 常见问题排查

| 问题 | 可能原因 | 解决方案 |
|------|---------|---------|
| API调用失败 | API Key无效/余额不足 | 检查API Key和账户余额 |
| 内存不足 | 数据量过大 | 减少batch_size或使用分批处理 |
| 结果为空 | 输入文件路径错误 | 检查文件路径和格式 |
| 评分异常 | 评估模型配置错误 | 检查EVAL_MODEL_NAME配置 |

---

## 七、实施路线图

### 7.1 整体时间规划（6个月）

```
┌─────────────────────────────────────────────────────────────────────┐
│                     实施路线图                                       │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  月份     1       2       3       4       5       6                │
│         ┌───────┬───────┬───────┬───────┬───────┬───────┐         │
│  阶段1  │███████│███████│       │       │       │       │         │
│  基础   │ • 多Agent架构设计                               │         │
│  建设   │ • 数据适配层实现                               │         │
│         │ • MCP服务搭建                                  │         │
│         └───────┴───────┴───────┴───────┴───────┴───────┘         │
│         ┌───────┬───────┬───────┬───────┬───────┬───────┐         │
│  阶段2  │       │███████│███████│███████│       │       │         │
│  模型   │       │ • 数据准备与增强                        │         │
│  训练   │       │ • SFT训练                               │         │
│         │       │ • RM训练                                │         │
│         │       │ • RAG知识库构建                         │         │
│         └───────┴───────┴───────┴───────┴───────┴───────┘         │
│         ┌───────┬───────┬───────┬───────┬───────┬───────┐         │
│  阶段3  │       │       │       │███████│███████│       │         │
│  乡村   │       │       │       │ • 文化知识图谱                  │
│  文化   │       │       │       │ • 文化Agent优化                 │
│         │       │       │       │ • 乡村场景测试                  │
│         └───────┴───────┴───────┴───────┴───────┴───────┘         │
│         ┌───────┬───────┬───────┬───────┬───────┬───────┐         │
│  阶段4  │       │       │       │       │███████│███████│         │
│  优化   │       │       │       │       │ • 多模态支持              │
│  上线   │       │       │       │       │ • RLHF优化                │
│         │       │       │       │       │ • 全面评估与上线          │
│         └───────┴───────┴───────┴───────┴───────┴───────┘         │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 7.2 各阶段关键交付物

| 阶段 | 时间 | 关键交付物 | 验收标准 |
|------|------|-----------|---------|
| 阶段1 | 月1-2 | 多Agent系统框架、数据适配层、MCP服务 | 系统可运行，API可调用 |
| 阶段2 | 月2-4 | SFT模型、RM模型、RAG知识库 | 功能性指标≥85% |
| 阶段3 | 月4-5 | 文化知识图谱、文化Agent | 文化融入度≥75% |
| 阶段4 | 月5-6 | 最终模型、评估报告 | 总分超越基线20%+ |

### 7.3 风险与应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| API成本超支 | 中 | 高 | 设置调用限额，优化缓存策略 |
| 训练效果不达标 | 中 | 高 | 增加数据量，调整超参数 |
| 乡村数据不足 | 高 | 中 | 数据采集，人工标注 |
| 多模态格式复杂 | 中 | 低 | 先支持图片，渐进扩展 |

---

## 附录

### A. 关键技术选型

| 组件 | 推荐选型 | 备选方案 |
|------|---------|---------|
| 基础模型 | DeepSeek-V2 | Qwen-72B, LLaMA-3 |
| 向量数据库 | Milvus | Pinecone, Weaviate |
| Embedding | BGE-large-zh | M3E, Text2Vec |
| 视觉模型 | BLIP-2 | Qwen-VL, LLaVA |
| ASR模型 | Whisper | FunASR |

### B. 参考资源

- DeepSpeed文档: https://www.deepspeed.ai/
- LangChain文档: https://python.langchain.com/
- Milvus文档: https://milvus.io/docs/

---

**文档版本**: v2.0  
**最后更新**: 2024  
**维护者**: 乡村文旅Agent项目组
