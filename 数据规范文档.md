# TripTailor 数据规范文档

## 文档目的

本文档定义了TripTailor旅行规划系统中新地点旅游数据的数据规范，确保新数据与训练集保持一致的格式，从而保证训练好的Agent能够正确处理和利用新数据。

## 1. 数据规范总则

### 1.1 核心原则

1. **格式一致性**: 所有新数据必须严格遵循训练集的字段名称、数据类型和格式规范
2. **完整性**: 核心字段必须完整，不允许缺失
3. **准确性**: 数据必须准确反映实际情况
4. **可扩展性**: 允许在保持核心字段一致的前提下扩展新字段

### 1.2 文件格式要求

- **CSV文件**: 使用UTF-8编码，逗号分隔，第一行为字段名
- **JSON文件**: 使用UTF-8编码，标准JSON格式
- **文件命名**: 必须与训练集保持一致（如 `accommodations.csv`, `restaurants.csv` 等）

### 1.3 通用数据类型规范

| 数据类型 | 格式要求 | 示例 |
|---------|---------|------|
| 字符串 | UTF-8编码，无特殊字符 | "Beijing Hotel" |
| 数值 | 浮点数或整数 | 106.5, 3.5 |
| 坐标 | WGS84坐标系，浮点数 | 116.377235, 39.866066 |
| 时间 | 24小时制 HH:MM | "09:00", "15:30" |
| 日期 | 星期几英文 | "Monday", "Tuesday" |
| 价格 | 人民币元，浮点数 | 200.0, 150.5 |

## 2. 各数据文件详细规范

### 2.1 住宿数据 (accommodations.csv)

#### 字段规范

| 字段名 | 类型 | 必填 | 说明 | 取值范围/格式 |
|--------|------|------|------|--------------|
| `name` | String | ✅ | 酒店/住宿名称 | 非空字符串，建议不超过100字符 |
| `real_city` | String | ✅ | 所在城市 | 城市名称，如 "Beijing", "Shanghai" |
| `avg_price` | Float | ✅ | 平均价格（元/晚） | > 0，建议范围 50-5000 |
| `small_cate` | String | ✅ | 住宿类别 | Economy/Midscale/Luxury/Upscale，必须与训练集类别一致 |
| `stars` | Float | ✅ | 星级评分 | 0.0-5.0，通常为 0.5 的倍数（如 3.0, 3.5, 4.0） |
| `review_count` | Integer | ✅ | 评论数量 | ≥ 0 |
| `good_remarks` | Float | ✅ | 好评数量 | ≥ 0，通常 ≤ review_count |
| `bad_remarks` | Float | ✅ | 差评数量 | ≥ 0，通常 ≤ review_count |
| `longitude` | Float | ✅ | 经度 | -180.0 到 180.0，WGS84坐标系 |
| `latitude` | Float | ✅ | 纬度 | -90.0 到 90.0，WGS84坐标系 |
| `product_rating` | Float | ✅ | 产品评分 | 0.0-10.0 |
| `environment_rating` | Float | ✅ | 环境评分 | 0.0-10.0 |
| `service_rating` | Float | ✅ | 服务评分 | 0.0-10.0 |

#### 数据示例

```csv
name,real_city,avg_price,small_cate,stars,review_count,good_remarks,bad_remarks,longitude,latitude,product_rating,environment_rating,service_rating
Grand Hotel Beijing,Beijing,850.0,Luxury,4.5,1250,1180.0,45.0,116.377235,39.866066,8.5,8.8,8.6
```

#### 验证规则

1. 所有字段必须存在且非空
2. `good_remarks + bad_remarks ≤ review_count`
3. `longitude` 和 `latitude` 必须在有效范围内
4. `small_cate` 必须在预定义类别列表中
5. 价格、评分必须在合理范围内

---

### 2.2 餐厅数据 (restaurants.csv)

#### 字段规范

| 字段名 | 类型 | 必填 | 说明 | 取值范围/格式 |
|--------|------|------|------|--------------|
| `name` | String | ✅ | 餐厅名称 | 非空字符串 |
| `real_city` | String | ✅ | 所在城市 | 城市名称 |
| `avg_price` | Float | ✅ | 平均价格（元/人） | > 0 |
| `small_cate` | String | ✅ | 餐厅类别 | 必须与训练集类别一致（如 Hot Pot, Western, Seafood, Sichuan Cuisine等） |
| `stars` | Float | ✅ | 星级评分 | 0.0-5.0 |
| `review_count` | Integer | ✅ | 评论数量 | ≥ 0 |
| `good_remarks` | Float | ✅ | 好评数量 | ≥ 0 |
| `bad_remarks` | Float | ✅ | 差评数量 | ≥ 0 |
| `longitude` | Float | ✅ | 经度 | -180.0 到 180.0 |
| `latitude` | Float | ✅ | 纬度 | -90.0 到 90.0 |
| `product_rating` | Float | ✅ | 产品评分 | 0.0-10.0 |
| `environment_rating` | Float | ✅ | 环境评分 | 0.0-10.0 |
| `service_rating` | Float | ✅ | 服务评分 | 0.0-10.0 |
| `nearby_attractions` | String | ⚠️ | 附近景点 | 分号（;）分隔的景点名称列表，可为空 |

#### 数据示例

```csv
name,real_city,avg_price,small_cate,stars,review_count,good_remarks,bad_remarks,longitude,latitude,product_rating,environment_rating,service_rating,nearby_attractions
Peking Duck Restaurant,Beijing,280.0,Beijing Cuisine,4.5,850,780.0,35.0,116.397128,39.916668,8.2,8.5,8.3,Forbidden City; Tiananmen Square; Jingshan Park
```

#### 验证规则

1. 所有必填字段必须存在
2. `nearby_attractions` 如果存在，必须使用分号分隔
3. `small_cate` 必须在预定义类别列表中
4. 坐标必须在有效范围内

---

### 2.3 航班时刻表 (Flight_Schedule.csv)

#### 字段规范

| 字段名 | 类型 | 必填 | 说明 | 取值范围/格式 |
|--------|------|------|------|--------------|
| `Departure City` | String | ✅ | 出发城市 | 城市名称 |
| `Arrival City` | String | ✅ | 到达城市 | 城市名称 |
| `Distance (km)` | Float | ✅ | 距离（公里） | > 0 |
| `Flight Number` | String | ✅ | 航班号 | 标准航班号格式（如 CA1276, CZ6919） |
| `Airline` | String | ✅ | 航空公司 | 航空公司全称 |
| `Aircraft Type` | String | ✅ | 机型 | 机型描述（如 "Boeing 737 (Medium)", "JET"） |
| `Departure Time` | String | ✅ | 出发时间 | HH:MM 格式（如 "15:40"） |
| `Arrival Time` | String | ✅ | 到达时间 | HH:MM 格式（如 "21:40"） |
| `Departure Airport` | String | ✅ | 出发机场 | 机场全称 |
| `Departure Airport Latitude` | Float | ✅ | 出发机场纬度 | -90.0 到 90.0 |
| `Departure Airport Longitude` | Float | ✅ | 出发机场经度 | -180.0 到 180.0 |
| `Arrival Airport` | String | ✅ | 到达机场 | 机场全称 |
| `Arrival Airport Latitude` | Float | ✅ | 到达机场纬度 | -90.0 到 90.0 |
| `Arrival Airport Longitude` | Float | ✅ | 到达机场经度 | -180.0 到 180.0 |
| `On-Time Performance` | Float | ✅ | 准点率 | 0.0-1.0（如 0.63 表示 63%） |
| `Average Delay (minutes)` | Integer | ✅ | 平均延误时间（分钟） | ≥ 0 |
| `Monday` | Integer | ✅ | 周一是否运行 | 1（运行）或 0（不运行） |
| `Tuesday` | Integer | ✅ | 周二是否运行 | 1 或 0 |
| `Wednesday` | Integer | ✅ | 周三是否运行 | 1 或 0 |
| `Thursday` | Integer | ✅ | 周四是否运行 | 1 或 0 |
| `Friday` | Integer | ✅ | 周五是否运行 | 1 或 0 |
| `Saturday` | Integer | ✅ | 周六是否运行 | 1 或 0 |
| `Sunday` | Integer | ✅ | 周日是否运行 | 1 或 0 |
| `Price` | Float | ✅ | 票价（元） | > 0 |

#### 数据示例

```csv
Departure City,Arrival City,Distance (km),Flight Number,Airline,Aircraft Type,Departure Time,Arrival Time,Departure Airport,Departure Airport Latitude,Departure Airport Longitude,Arrival Airport,Arrival Airport Latitude,Arrival Airport Longitude,On-Time Performance,Average Delay (minutes),Monday,Tuesday,Wednesday,Thursday,Friday,Saturday,Sunday,Price
Beijing,Shanghai,1178,CA1234,Air China,Boeing 737 (Medium),08:00,10:30,Capital Airport,40.06248537,116.5992671,Pudong Airport,31.143378,121.805214,0.85,15,1,1,1,1,1,1,1,1200
```

#### 验证规则

1. 所有字段必须存在
2. 时间格式必须为 HH:MM
3. 至少有一个运行日期为 1
4. 出发和到达时间必须合理（到达时间晚于出发时间，考虑时区）
5. 机场坐标必须准确

---

### 2.4 火车时刻表 (Train_Schedule.csv)

#### 字段规范

| 字段名 | 类型 | 必填 | 说明 | 取值范围/格式 |
|--------|------|------|------|--------------|
| `Train_Number` | String | ✅ | 车次号 | 标准车次格式（如 G1, G10, D101） |
| `Station_Number` | Integer | ✅ | 站点序号 | ≥ 1，同一车次内递增 |
| `Station_Name` | String | ✅ | 站点名称 | 火车站全称 |
| `Arrival_Time` | String | ✅ | 到达时间 | HH:MM 或 "departure station"（始发站） |
| `Departure_Time` | String | ✅ | 出发时间 | HH:MM 或 "terminal station"（终点站） |
| `Stop_Time` | Integer | ✅ | 停靠时间（分钟） | ≥ 0，始发站和终点站为 0 |
| `Running_Time` | Integer | ✅ | 运行时间（分钟） | ≥ 0，始发站为 0 |
| `Second_Class_Price` | Float | ✅ | 二等座价格（元） | ≥ 0，始发站为 0 |
| `First_Class_Price` | Float | ✅ | 一等座价格（元） | ≥ 0，始发站为 0 |
| `Longitude` | Float | ✅ | 站点经度 | -180.0 到 180.0 |
| `Latitude` | Float | ✅ | 站点纬度 | -90.0 到 90.0 |

#### 数据示例

```csv
Train_Number,Station_Number,Station_Name,Arrival_Time,Departure_Time,Stop_Time,Running_Time,Second_Class_Price,First_Class_Price,Longitude,Latitude
G101,1,Beijing South Railway Station,departure station,09:00,0,0,0,0,116.377235,39.866066
G101,2,Tianjin West Railway Station,09:30,09:32,2,30,54.5,99.5,117.210813,39.14393
G101,3,Shanghai Hongqiao Station,13:28,terminal station,0,268,553,933,121.320081,31.193964
```

#### 验证规则

1. 同一车次的站点必须按 `Station_Number` 递增
2. 始发站的 `Arrival_Time` 必须为 "departure station"
3. 终点站的 `Departure_Time` 必须为 "terminal station"
4. 运行时间必须递增
5. 价格必须递增（从始发站到终点站）

---

### 2.5 景点数据 (attractions.csv)

#### 字段规范

| 字段名 | 类型 | 必填 | 说明 | 取值范围/格式 |
|--------|------|------|------|--------------|
| `poiId` | String/Integer | ✅ | 景点ID | 唯一标识符 |
| `city` | String | ✅ | 所在城市 | 城市名称 |
| `poiName` | String | ✅ | 景点名称 | 非空字符串 |
| `commentCount` | Integer | ✅ | 评论数量 | ≥ 0 |
| `commentScore` | Float | ✅ | 评论评分 | 0.0-5.0 |
| `heatScore` | Float | ✅ | 热度评分 | ≥ 0 |
| `sightLevelStr` | String | ⚠️ | 景点等级 | 可为空，如 "5A", "4A" 等 |
| `price` | Float | ✅ | 门票价格（元） | ≥ 0，免费为 0.0 |
| `latitude` | Float | ✅ | 纬度 | -90.0 到 90.0 |
| `longitude` | Float | ✅ | 经度 | -180.0 到 180.0 |
| `tagNameList` | String | ⚠️ | 标签列表 | 分号（;）分隔，可为空 |
| `shortFeatures` | String | ⚠️ | 简短特色描述 | 可为空 |
| `reference_time` | String | ⚠️ | 推荐游览时长 | 如 "1-2 hours", "2-3 hours"，可为空 |
| `summary` | String | ⚠️ | 景点详细介绍 | 可为空，支持多行文本 |
| `opening_hours` | String | ⚠️ | 开放时间 | 如 "08:30-17:00", "Open year round, 24/7"，可为空 |

#### 数据示例

```csv
poiId,city,poiName,commentCount,commentScore,heatScore,sightLevelStr,price,latitude,longitude,tagNameList,shortFeatures,reference_time,summary,opening_hours
10001,Beijing,Forbidden City,50000,4.7,9.5,5A,60.0,39.916668,116.397128,Historical Sites; Museums; World Heritage,Imperial Palace of Ming and Qing Dynasties,3-4 hours,"The Forbidden City is the imperial palace of the Ming and Qing dynasties...",08:30-17:00
```

#### 验证规则

1. `poiId` 必须唯一
2. 坐标必须在有效范围内
3. `tagNameList` 如果存在，必须使用分号分隔
4. `summary` 如果包含换行，CSV中需要用引号包裹

---

### 2.6 训练数据 (train.json)

#### 字段规范

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `destination_city` | String | ✅ | 目的地城市 |
| `departure_city` | String | ✅ | 出发城市 |
| `day` | Integer | ✅ | 旅行天数，≥ 1 |
| `pid` | String/Integer | ✅ | 计划ID，唯一标识符 |
| `query` | String | ✅ | 用户查询文本，自然语言描述 |
| `final_plan` | Array[String] | ✅ | 最终旅行计划，字符串数组，每个元素为一天的详细行程 |

#### 数据示例

```json
{
    "destination_city": "Beijing",
    "departure_city": "Shanghai",
    "day": 3,
    "pid": "plan_001",
    "query": "I am looking for a 3-day trip from Shanghai to Beijing, departing on Friday morning and returning on Sunday evening, with a budget of ¥5000. I prefer staying in luxury hotels and dining at restaurants with meal costs over ¥200.",
    "final_plan": [
        "**Day 1 Itinerary: Shanghai to Beijing**\n\n### 08:00–10:30 | Travel to Beijing...",
        "**Day 2 Itinerary**\n\n### 09:00–12:00 | Visit Forbidden City...",
        "**Day 3 Itinerary**\n\n### 10:00–12:00 | Visit Temple of Heaven..."
    ]
}
```

---

### 2.7 测试数据 (test.json)

#### 字段规范

测试数据包含训练数据的所有字段，并额外包含：

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `meal_price_range` | Array[Float] | ✅ | 餐费价格范围，如 [50, 100] |
| `budget` | Float | ✅ | 总预算（元） |
| `final_plan_json` | String | ✅ | 最终计划的JSON格式字符串，包含结构化的酒店、交通、行程信息 |

#### 数据示例

```json
{
    "destination_city": "Beijing",
    "departure_city": "Shanghai",
    "day": 3,
    "pid": "test_001",
    "meal_price_range": [100, 200],
    "budget": 5000,
    "query": "...",
    "final_plan": [...],
    "final_plan_json": "{\"hotel\": [...], \"transportation\": [...], \"itinerary\": {...}}"
}
```

---

## 3. 数据质量标准

### 3.1 完整性标准

1. **必填字段完整性**: 所有标记为 ✅ 的字段必须存在且非空
2. **关联数据完整性**: 
   - 餐厅的 `nearby_attractions` 中的景点必须在 `attractions.csv` 中存在
   - 交通数据中的城市必须在其他数据文件中存在
3. **时间逻辑完整性**: 
   - 航班/火车的到达时间必须晚于出发时间
   - 同一车次的站点时间必须递增

### 3.2 准确性标准

1. **坐标准确性**: 经纬度坐标必须准确反映实际位置
2. **价格合理性**: 价格应在合理范围内，避免异常值
3. **时间准确性**: 时刻表时间必须准确
4. **评分一致性**: 评分数据应与实际评价一致

### 3.3 一致性标准

1. **命名一致性**: 城市名称在所有文件中必须一致（如统一使用 "Beijing" 而非 "北京"）
2. **类别一致性**: 类别值必须在预定义列表中
3. **格式一致性**: 时间、价格、坐标等格式必须统一

### 3.4 数据量要求

对于新城市，建议至少包含：
- **住宿**: ≥ 50 条记录
- **餐厅**: ≥ 200 条记录
- **景点**: ≥ 20 条记录
- **交通**: 至少包含该城市与其他主要城市的航班/火车连接

## 4. 数据扩展性说明

### 4.1 允许的扩展

在保持核心字段一致的前提下，可以：

1. **添加新字段**: 可以在CSV中添加新列，但必须：
   - 不影响现有字段
   - 在数据处理时能够处理缺失值
   - 文档化新字段的用途

2. **扩展类别**: 可以添加新的类别值，但需要：
   - 确保类别名称清晰明确
   - 在模型训练时能够处理新类别
   - 建立类别映射规则

3. **扩展JSON结构**: 可以在JSON中添加新字段，但必须：
   - 保持向后兼容
   - 不影响现有字段的解析

### 4.2 不允许的修改

1. **字段名称**: 不能修改现有字段名称
2. **数据类型**: 不能改变字段的数据类型
3. **必填字段**: 不能将必填字段改为可选
4. **核心格式**: 不能改变时间、坐标、价格等核心格式

## 5. 数据验证方法

### 5.1 自动化验证脚本

建议创建数据验证脚本，检查以下内容：

```python
# 伪代码示例
def validate_data(data_file, schema):
    """
    验证数据文件是否符合规范
    """
    checks = [
        check_required_fields(),      # 检查必填字段
        check_data_types(),           # 检查数据类型
        check_value_ranges(),         # 检查取值范围
        check_format_consistency(),   # 检查格式一致性
        check_logical_consistency(),  # 检查逻辑一致性
        check_referential_integrity() # 检查引用完整性
    ]
    return all(checks)
```

### 5.2 验证检查清单

#### CSV文件验证
- [ ] 文件编码为 UTF-8
- [ ] 字段名与规范一致
- [ ] 所有必填字段存在且非空
- [ ] 数据类型正确
- [ ] 数值在合理范围内
- [ ] 时间格式正确
- [ ] 坐标格式正确
- [ ] 分隔符使用正确（逗号）

#### JSON文件验证
- [ ] JSON格式有效
- [ ] 文件编码为 UTF-8
- [ ] 所有必填字段存在
- [ ] 数据类型正确
- [ ] 数组和对象结构正确

#### 跨文件验证
- [ ] 城市名称在所有文件中一致
- [ ] 引用关系正确（如餐厅的附近景点确实存在）
- [ ] 交通数据中的城市在其他数据中存在

### 5.3 数据质量报告

验证后应生成数据质量报告，包括：
- 总记录数
- 缺失值统计
- 异常值统计
- 格式错误统计
- 逻辑错误统计
- 通过率

## 6. 数据导入流程

### 6.1 准备阶段

1. **数据收集**: 收集新城市的旅游数据
2. **数据清洗**: 清理和标准化数据
3. **格式转换**: 转换为规范格式

### 6.2 验证阶段

1. **格式验证**: 运行自动化验证脚本
2. **质量检查**: 检查数据质量报告
3. **人工审核**: 抽样检查数据准确性

### 6.3 导入阶段

1. **备份**: 备份现有数据
2. **合并**: 将新数据合并到现有数据集
3. **去重**: 去除重复记录
4. **索引**: 更新索引和引用关系

### 6.4 测试阶段

1. **功能测试**: 使用新数据测试Agent功能
2. **性能测试**: 检查性能是否受影响
3. **准确性测试**: 验证生成的计划是否合理

## 7. 常见问题与解决方案

### 7.1 数据缺失问题

**问题**: 某些字段数据缺失

**解决方案**:
- 必填字段: 必须补充数据或使用合理的默认值
- 可选字段: 可以使用空值或占位符

### 7.2 格式不一致问题

**问题**: 时间、价格等格式不一致

**解决方案**:
- 建立格式转换规则
- 使用数据清洗脚本统一格式

### 7.3 类别不匹配问题

**问题**: 新数据的类别不在训练集类别列表中

**解决方案**:
- 建立类别映射表
- 将新类别映射到最相似的现有类别
- 或扩展类别列表（需重新训练模型）

### 7.4 坐标系统问题

**问题**: 坐标系统不一致

**解决方案**:
- 统一使用 WGS84 坐标系
- 使用坐标转换工具转换其他坐标系

## 8. 最佳实践

### 8.1 数据收集

1. **多源验证**: 从多个数据源收集数据，交叉验证
2. **实时更新**: 定期更新数据，保持时效性
3. **质量控制**: 建立数据质量检查机制

### 8.2 数据维护

1. **版本控制**: 使用版本控制管理数据变更
2. **变更日志**: 记录数据变更历史
3. **定期审核**: 定期审核数据质量和准确性

### 8.3 文档化

1. **数据字典**: 维护完整的数据字典
2. **变更说明**: 记录数据变更和扩展
3. **使用指南**: 提供数据使用指南和示例

## 9. 附录

### 9.1 预定义类别列表

#### 住宿类别
- Economy (经济型)
- Midscale (中档)
- Upscale (高档)
- Luxury (豪华)

#### 餐厅类别（部分）
- Hot Pot (火锅)
- Western (西餐)
- Seafood (海鲜)
- Sichuan Cuisine (川菜)
- Beijing Cuisine (京菜)
- Cantonese Cuisine (粤菜)
- Japanese (日料)
- Korean (韩料)
- Fast Food (快餐)
- Buffet (自助餐)
- Pizza (披萨)
- ... (更多类别见训练集)

### 9.2 数据验证工具推荐

- **Python**: pandas, jsonschema, cerberus
- **命令行**: csvkit, jq
- **数据库**: 使用数据库约束和触发器

### 9.3 联系方式

如有数据规范相关问题，请联系数据团队或查阅项目文档。

---

**文档版本**: v1.0  
**最后更新**: 2024  
**维护者**: TripTailor 数据团队

